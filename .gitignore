# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env
.env*.local
!.env.example

# typescript
*.tsbuildinfo

# generated native folders
/ios
/android

# editors
.idea/
.vscode/

# Play Store submission — the service account key can publish releases
play-service-account.json
*-service-account.json

# Working notes and pre-decision analysis, kept out of the published history.
AGENTS.md
CLAUDE.md
/.claude/

# The store listing is copy for our own Play entry, in three languages against
# Play's character limits. Anyone publishing their own build writes their own.
/docs/play-listing.md

# A database pulled off a device for debugging holds real interview records,
# audio and photographs. It must never be committable.
*.db
*.sqlite
*.sqlite3
*.csv
*.xlsx
